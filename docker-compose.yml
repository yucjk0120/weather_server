services:
  weather-server:
    build: .
    ports:
      - "8765:8000"
    volumes:
      - ./data:/app/data
      - ./app/templates:/app/app/templates
      - ./app/static:/app/app/static
    restart: unless-stopped
